# Generated by spaday.cem from custom-elements.json — do not edit by hand.
from __future__ import annotations

from typing import Any, Literal

from spaday.catalog import ComponentSchema, PropertySchema
from spaday.component import Child, Component

__all__ = [
    "BpAccordion",
    "BpAccordionContent",
    "BpAccordionHeader",
    "BpAccordionPanel",
    "BpAlert",
    "BpAlertGroup",
    "BpAvatar",
    "BpBadge",
    "BpBreadcrumb",
    "BpButton",
    "BpButtonCopy",
    "BpButtonExpand",
    "BpButtonGroup",
    "BpButtonHandle",
    "BpButtonIcon",
    "BpButtonResize",
    "BpButtonSort",
    "BpCard",
    "BpChatGroup",
    "BpChatMessage",
    "BpCheckbox",
    "BpColor",
    "BpDate",
    "BpDialog",
    "BpDivider",
    "BpDrawer",
    "BpDropdown",
    "BpField",
    "BpFieldMessage",
    "BpFieldset",
    "BpFile",
    "BpFormGroup",
    "BpFormatBytes",
    "BpFormatDatetime",
    "BpFormatNumber",
    "BpFormatRelativeTime",
    "BpFormatToken",
    "BpHeader",
    "BpHeaderItem",
    "BpIcon",
    "BpInput",
    "BpMenu",
    "BpMenuItem",
    "BpMonth",
    "BpNav",
    "BpNavGroup",
    "BpNavItem",
    "BpNumber",
    "BpNumberStepper",
    "BpOption",
    "BpPage",
    "BpPagination",
    "BpPaginationInput",
    "BpPanel",
    "BpPassword",
    "BpPin",
    "BpProgressBar",
    "BpProgressCircle",
    "BpProgressDot",
    "BpRadio",
    "BpRange",
    "BpRating",
    "BpSearch",
    "BpSelect",
    "BpSkeleton",
    "BpStepper",
    "BpStepperItem",
    "BpSwitch",
    "BpTab",
    "BpTabList",
    "BpTabPanel",
    "BpTabs",
    "BpTag",
    "BpTelephone",
    "BpTextarea",
    "BpTime",
    "BpToast",
    "BpToggletip",
    "BpTooltip",
    "BpTree",
    "BpTreeItem",
]


class BpAccordionContent(Component):
    """Accordion Content"""

    tag = "bp-accordion-content"
    schema = ComponentSchema(
        tag="bp-accordion-content", class_name="BpAccordionContent", summary="Accordion Content", props=(), events=(), slots=("",)
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpAccordion(Component):
    """The accordion component is used to display a large amount of content in a small space. The accordion should be used to group related content together."""

    tag = "bp-accordion"
    schema = ComponentSchema(
        tag="bp-accordion",
        class_name="BpAccordion",
        summary="The accordion component is used to display a large amount of content in a small space. The accordion should be used to group related content together.",
        props=(
            PropertySchema(
                name="layer",
                kind="enum",
                choices=("flat", "container"),
                default="container",
                description="Determines the visual layer style, with 'container' providing backgrounds and borders, while 'flat' removes them for nested contexts",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        layer: Literal["flat", "container"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "layer": layer,
            },
            **props,
        )


class BpAccordionHeader(Component):
    """Accordion Header"""

    tag = "bp-accordion-header"
    schema = ComponentSchema(
        tag="bp-accordion-header",
        class_name="BpAccordionHeader",
        summary="Accordion Header",
        props=(
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpAccordionPanel(Component):
    """Accordion Panel"""

    tag = "bp-accordion-panel"
    schema = ComponentSchema(
        tag="bp-accordion-panel",
        class_name="BpAccordionPanel",
        summary="Accordion Panel",
        props=(PropertySchema(name="expanded", kind="boolean", choices=(), default="false", description="determine if element is expanded"),),
        events=(),
        slots=("", "bp-accordion-header", "bp-accordion-content"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        expanded: bool | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "expanded": expanded,
            },
            **props,
        )


class BpAlert(Component):
    """The alert component is used to inform users of important information or to provide feedback on an action they have taken."""

    tag = "bp-alert"
    schema = ComponentSchema(
        tag="bp-alert",
        class_name="BpAlert",
        summary="The alert component is used to inform users of important information or to provide feedback on an action they have taken.",
        props=(
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type of the alert, affecting its color, icon, and semantic meaning",
            ),
            PropertySchema(
                name="closable",
                kind="boolean",
                choices=(),
                default="false",
                description="Controls whether the alert displays a close button, allowing users to dismiss it",
            ),
            PropertySchema(
                name="hidden",
                kind="boolean",
                choices=(),
                default="false",
                description="Controls the visibility state of the alert, hiding it from view when set to true",
            ),
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        closable: bool | None = None,
        hidden: bool | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "status": status,
                "closable": closable,
                "hidden": hidden,
                "i18n": i18n,
            },
            **props,
        )


class BpAlertGroup(Component):
    """The alert group component is used to inform users of important information or to provide feedback on an action they have taken."""

    tag = "bp-alert-group"
    schema = ComponentSchema(
        tag="bp-alert-group",
        class_name="BpAlertGroup",
        summary="The alert group component is used to inform users of important information or to provide feedback on an action they have taken.",
        props=(
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="determine the visual status state",
            ),
            PropertySchema(
                name="type", kind="enum", choices=("banner",), default=None, description="determine the visual styles for top/pinned banners"
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        type: Literal["banner"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "status": status,
                "type": type,
            },
            **props,
        )


class BpAvatar(Component):
    """The avatar component is used to represent a user or entity with an image, icon, or initials"""

    tag = "bp-avatar"
    schema = ComponentSchema(
        tag="bp-avatar",
        class_name="BpAvatar",
        summary="The avatar component is used to represent a user or entity with an image, icon, or initials",
        props=(
            PropertySchema(
                name="shape", kind="enum", choices=("square", "rounded"), default=None, description="determine the visual shape of the avatar"
            ),
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="optional status indicator color",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        shape: Literal["square", "rounded"] | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "shape": shape,
                "status": status,
            },
            **props,
        )


class BpBadge(Component):
    """The badge component is used to display a small amount of information, such as a count or status, in a compact and visually distinct way. It is often used to display notifications or unread messages."""

    tag = "bp-badge"
    schema = ComponentSchema(
        tag="bp-badge",
        class_name="BpBadge",
        summary="The badge component is used to display a small amount of information, such as a count or status, in a compact and visually distinct way. It is often used to display notifications or unread messages.",
        props=(
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type of the badge, affecting its color and semantic meaning",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "status": status,
            },
            **props,
        )


class BpBreadcrumb(Component):
    """The breadcrumb component is used to show the user's current location, and provides an easy way to navigate back to previous sections."""

    tag = "bp-breadcrumb"
    schema = ComponentSchema(
        tag="bp-breadcrumb",
        class_name="BpBreadcrumb",
        summary="The breadcrumb component is used to show the user's current location, and provides an easy way to navigate back to previous sections.",
        props=(),
        events=(),
        slots=("", "separator"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpButtonCopy(Component):
    """The button copy component provides a simple way to copy text to the clipboard with visual feedback."""

    tag = "bp-button-copy"
    schema = ComponentSchema(
        tag="bp-button-copy",
        class_name="BpButtonCopy",
        summary="The button copy component provides a simple way to copy text to the clipboard with visual feedback.",
        props=(
            PropertySchema(name="value", kind="string", choices=(), default="", description="The text value to copy to clipboard"),
            PropertySchema(name="i18n", kind="json", choices=(), default=None, description="set default aria/i18n strings"),
            PropertySchema(
                name="shape",
                kind="string",
                choices=(),
                default="ellipsis-vertical",
                description="Defines the icon shape to display within the button",
            ),
            PropertySchema(
                name="direction",
                kind="enum",
                choices=("up", "down", "left", "right"),
                default=None,
                description="Controls the directional orientation of the icon within the button",
            ),
            PropertySchema(
                name="action",
                kind="enum",
                choices=("primary", "secondary", "flat", "inline"),
                default=None,
                description="Controls the visual styling variant of the button, affecting background, border, and emphasis level",
            ),
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type of the button, affecting its color and semantic meaning",
            ),
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=("copy",),
        slots=("", "success", "error"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        value: str | None = None,
        i18n: Any = None,
        shape: str | None = None,
        direction: Literal["up", "down", "left", "right"] | None = None,
        action: Literal["primary", "secondary", "flat", "inline"] | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "value": value,
                "i18n": i18n,
                "shape": shape,
                "direction": direction,
                "action": action,
                "status": status,
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpButtonExpand(Component):
    """The expand button component is used to reveal and hide additional content, such as a dropdown menu or a nested list."""

    tag = "bp-button-expand"
    schema = ComponentSchema(
        tag="bp-button-expand",
        class_name="BpButtonExpand",
        summary="The expand button component is used to reveal and hide additional content, such as a dropdown menu or a nested list.",
        props=(
            PropertySchema(
                name="orientation",
                kind="enum",
                choices=("vertical", "horizontal"),
                default="vertical",
                description="Controls the icon direction based on expand context, either vertical (down/right) or horizontal (left/right)",
            ),
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                type_text="I18nStrings['actions']",
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
        ),
        events=("input", "change"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        orientation: Literal["vertical", "horizontal"] | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "orientation": orientation,
                "i18n": i18n,
            },
            **props,
        )


class BpButtonGroup(Component):
    """The button group component is used to group together related buttons and present them as a single visual unit."""

    tag = "bp-button-group"
    schema = ComponentSchema(
        tag="bp-button-group",
        class_name="BpButtonGroup",
        summary="The button group component is used to group together related buttons and present them as a single visual unit.",
        props=(
            PropertySchema(
                name="action",
                kind="enum",
                choices=("primary", "secondary", "flat"),
                default=None,
                description="Controls the visual styling variant applied to all buttons within the group",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        action: Literal["primary", "secondary", "flat"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "action": action,
            },
            **props,
        )


class BpButtonHandle(Component):
    """The button handle component is used to act as a handle to drag and move an element, such as a slidebar or a modal window."""

    tag = "bp-button-handle"
    schema = ComponentSchema(
        tag="bp-button-handle",
        class_name="BpButtonHandle",
        summary="The button handle component is used to act as a handle to drag and move an element, such as a slidebar or a modal window.",
        props=(
            PropertySchema(
                name="shape",
                kind="string",
                choices=(),
                default="drag-handle",
                description="Defines the icon shape used for the drag handle visual indicator",
            ),
            PropertySchema(
                name="direction",
                kind="enum",
                choices=("up", "down", "left", "right"),
                default=None,
                description="Controls the directional orientation of the drag handle icon",
            ),
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        shape: str | None = None,
        direction: Literal["up", "down", "left", "right"] | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "shape": shape,
                "direction": direction,
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpButtonIcon(Component):
    """The icon button component is used to provide a visual representation of an action."""

    tag = "bp-button-icon"
    schema = ComponentSchema(
        tag="bp-button-icon",
        class_name="BpButtonIcon",
        summary="The icon button component is used to provide a visual representation of an action.",
        props=(
            PropertySchema(
                name="shape",
                kind="string",
                choices=(),
                default="ellipsis-vertical",
                description="Defines the icon shape to display within the button",
            ),
            PropertySchema(
                name="direction",
                kind="enum",
                choices=("up", "down", "left", "right"),
                default=None,
                description="Controls the directional orientation of the icon within the button",
            ),
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
            PropertySchema(
                name="action",
                kind="enum",
                choices=("primary", "secondary", "flat", "inline"),
                default=None,
                description="Controls the visual styling variant of the button, affecting background, border, and emphasis level",
            ),
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type of the button, affecting its color and semantic meaning",
            ),
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        shape: str | None = None,
        direction: Literal["up", "down", "left", "right"] | None = None,
        i18n: Any = None,
        action: Literal["primary", "secondary", "flat", "inline"] | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "shape": shape,
                "direction": direction,
                "i18n": i18n,
                "action": action,
                "status": status,
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpButtonResize(Component):
    """The resize button component is used to act as a drag handle to resize an element. Key navigation is supported and the step value can be used to adjust the amount of change when using the arrow keys."""

    tag = "bp-button-resize"
    schema = ComponentSchema(
        tag="bp-button-resize",
        class_name="BpButtonResize",
        summary="The resize button component is used to act as a drag handle to resize an element. Key navigation is supported and the step value can be used to adjust the amount of change when using the arrow keys.",
        props=(),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpButtonSort(Component):
    """The sort button component is used to allow users to sort a list of items by a specific criteria. It is typically used in tables, lists, or other data focused components."""

    tag = "bp-button-sort"
    schema = ComponentSchema(
        tag="bp-button-sort",
        class_name="BpButtonSort",
        summary="The sort button component is used to allow users to sort a list of items by a specific criteria. It is typically used in tables, lists, or other data focused components.",
        props=(
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                type_text="I18nStrings['actions']",
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
        ),
        fields=(
            PropertySchema(
                name="value",
                kind="json",
                choices=(),
                type_text="ButtonSort",
                default=None,
                description="Defines the current sort direction state, cycling through none, ascending, and descending",
            ),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="Sets the button as read-only, preventing sort state changes while maintaining focusability",
            ),
        ),
        events=("input", "change"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "i18n": i18n,
            },
            **props,
        )


class BpButton(Component):
    """The button component should be used when the user needs to take an action. The text or icon used in the button should be clear and concise, communicating the action the button will take."""

    tag = "bp-button"
    schema = ComponentSchema(
        tag="bp-button",
        class_name="BpButton",
        summary="The button component should be used when the user needs to take an action. The text or icon used in the button should be clear and concise, communicating the action the button will take.",
        props=(
            PropertySchema(
                name="action",
                kind="enum",
                choices=("primary", "secondary", "flat", "inline"),
                default=None,
                description="Controls the visual styling variant of the button, affecting background, border, and emphasis level",
            ),
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type of the button, affecting its color and semantic meaning",
            ),
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        action: Literal["primary", "secondary", "flat", "inline"] | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "action": action,
                "status": status,
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpCard(Component):
    """The card component is used to group related content together in a visually organized way. The card should have a clear and consistent structure. The header should contain content that describes the content of the card."""

    tag = "bp-card"
    schema = ComponentSchema(
        tag="bp-card",
        class_name="BpCard",
        summary="The card component is used to group related content together in a visually organized way. The card should have a clear and consistent structure. The header should contain content that describes the content of the card.",
        props=(),
        events=(),
        slots=("", "header", "footer"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpChatMessage(Component):
    """The Chat message is a component designed to display chat messages in a conversational format. It improves readability and provides a visual context for conversations."""

    tag = "bp-chat-message"
    schema = ComponentSchema(
        tag="bp-chat-message",
        class_name="BpChatMessage",
        summary="The Chat message is a component designed to display chat messages in a conversational format. It improves readability and provides a visual context for conversations.",
        props=(
            PropertySchema(
                name="type",
                kind="enum",
                choices=("sent", "received"),
                default=None,
                description="Defines the message type, determining alignment and styling for sent or received messages",
            ),
            PropertySchema(
                name="color",
                kind="enum",
                choices=("blue", "green", "red", "yellow", "purple"),
                default=None,
                description="Sets the base color variant for distinguishing different participants in multi-user chat groups",
            ),
            PropertySchema(
                name="arrow",
                kind="json",
                choices=(),
                type_text="Position",
                default=None,
                description="Controls the position of the speech bubble arrow relative to the message container",
            ),
            PropertySchema(
                name="progress",
                kind="boolean",
                choices=(),
                default=None,
                description="Controls whether to display a typing or progress spinner instead of message content",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: Literal["sent", "received"] | None = None,
        color: Literal["blue", "green", "red", "yellow", "purple"] | None = None,
        arrow: Any = None,
        progress: bool | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
                "color": color,
                "arrow": arrow,
                "progress": progress,
            },
            **props,
        )


class BpChatGroup(Component):
    """```typescript import '@blueprintui/components/include/chat.js'; ``` ```html <bp-chat-group> <bp-chat-message type="sent">How are you?</bp-chat-message> <bp-chat-message type="received">Great!</bp-chat-message> </bp-chat-group> ```"""

    tag = "bp-chat-group"
    schema = ComponentSchema(
        tag="bp-chat-group",
        class_name="BpChatGroup",
        summary='```typescript\nimport \'@blueprintui/components/include/chat.js\';\n```\n\n```html\n<bp-chat-group>\n  <bp-chat-message type="sent">How are you?</bp-chat-message>\n  <bp-chat-message type="received">Great!</bp-chat-message>\n</bp-chat-group>\n```',
        props=(),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpCheckbox(Component):
    """Checkboxes are used to select one or more options from a list. They are not intended for lists where only one option can be selected. Each checkbox can be selected independently of the others."""

    tag = "bp-checkbox"
    schema = ComponentSchema(
        tag="bp-checkbox",
        class_name="BpCheckbox",
        summary="Checkboxes are used to select one or more options from a list. They are not intended for lists where only one option can be selected. Each checkbox can be selected independently of the others.",
        props=(),
        events=("change",),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpColor(Component):
    """The color input component is used to allow users to select a color from a predefined set of options or to input a specific color value."""

    tag = "bp-color"
    schema = ComponentSchema(
        tag="bp-color",
        class_name="BpColor",
        summary="The color input component is used to allow users to select a color from a predefined set of options or to input a specific color value.",
        props=(
            PropertySchema(
                name="type",
                kind="string",
                choices=(),
                default="color",
                description="Defines the input type as color, enabling color picker functionality",
            ),
        ),
        events=("input", "change"),
        slots=("prefix", "suffix"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
            },
            **props,
        )


class BpDate(Component):
    """The date input component is used to allow users to select a date from the native browser datepicker input type."""

    tag = "bp-date"
    schema = ComponentSchema(
        tag="bp-date",
        class_name="BpDate",
        summary="The date input component is used to allow users to select a date from the native browser datepicker input type.",
        props=(
            PropertySchema(
                name="type",
                kind="string",
                choices=(),
                default="date",
                description="Defines the input type as date, enabling native browser date picker functionality",
            ),
        ),
        fields=(PropertySchema(name="valueAsDate", kind="json", choices=(), default=None, description=None),),
        events=("input", "change"),
        slots=("prefix", "suffix"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
            },
            **props,
        )


class BpDialog(Component):
    """The dialog component is used to display content in a overlay that appears on top of the current view. It is used to display information that requires the user's attention or interaction."""

    tag = "bp-dialog"
    schema = ComponentSchema(
        tag="bp-dialog",
        class_name="BpDialog",
        summary="The dialog component is used to display content in a overlay that appears on top of the current view. It is used to display information that requires the user's attention or interaction.",
        props=(
            PropertySchema(
                name="size",
                kind="enum",
                choices=("sm", "md", "lg"),
                default=None,
                description="Determines the visual size variant of the dialog, affecting width and content scaling",
            ),
            PropertySchema(
                name="position",
                kind="json",
                choices=(),
                type_text="Position",
                default="center",
                description="Controls the position of the dialog relative to the viewport",
            ),
            PropertySchema(
                name="closable",
                kind="boolean",
                choices=(),
                default="false",
                description="Controls whether the dialog displays a close button, allowing users to dismiss it",
            ),
            PropertySchema(
                name="modal",
                kind="boolean",
                choices=(),
                default="false",
                description="Controls whether the dialog is modal with a backdrop layer that prevents interaction with underlying content",
            ),
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
        ),
        events=("open", "close"),
        slots=("", "header", "footer"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        size: Literal["sm", "md", "lg"] | None = None,
        position: Any = None,
        closable: bool | None = None,
        modal: bool | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "size": size,
                "position": position,
                "closable": closable,
                "modal": modal,
                "i18n": i18n,
            },
            **props,
        )


class BpDivider(Component):
    """The divider component is used to visually separate content into distinct sections. It can be used to separate a group of related items or to indicate a change in content or context."""

    tag = "bp-divider"
    schema = ComponentSchema(
        tag="bp-divider",
        class_name="BpDivider",
        summary="The divider component is used to visually separate content into distinct sections. It can be used to separate a group of related items or to indicate a change in content or context.",
        props=(
            PropertySchema(
                name="orientation",
                kind="enum",
                choices=("horizontal", "vertical"),
                default="horizontal",
                description="Controls the layout direction of the divider, either horizontal or vertical",
            ),
        ),
        fields=(
            PropertySchema(name="role", kind="string", choices=(), default="separator", description=None),
            PropertySchema(name="ariaOrientation", kind="string", choices=(), default="horizontal", description=None),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        orientation: Literal["horizontal", "vertical"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "orientation": orientation,
            },
            **props,
        )


class BpDrawer(Component):
    """The drawer component is used to provide a hidden off-screen section that can be revealed by the user. It is commonly used to provide additional navigation or content that is not immediately necessary, but can be easily accessed when needed."""

    tag = "bp-drawer"
    schema = ComponentSchema(
        tag="bp-drawer",
        class_name="BpDrawer",
        summary="The drawer component is used to provide a hidden off-screen section that can be revealed by the user. It is commonly used to provide additional navigation or content that is not immediately necessary, but can be easily accessed when needed.",
        props=(
            PropertySchema(
                name="closable",
                kind="boolean",
                choices=(),
                default="false",
                description="Controls whether the drawer displays a close button, allowing users to dismiss it",
            ),
            PropertySchema(
                name="position",
                kind="enum",
                choices=("left", "right"),
                default="left",
                description="Controls the drawer position relative to the viewport, sliding in from the left or right edge",
            ),
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
        ),
        events=("open", "close"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        closable: bool | None = None,
        position: Literal["left", "right"] | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "closable": closable,
                "position": position,
                "i18n": i18n,
            },
            **props,
        )


class BpDropdown(Component):
    """The dropdown are a is a generic popup component that can be positioned relative to an anchor element."""

    tag = "bp-dropdown"
    schema = ComponentSchema(
        tag="bp-dropdown",
        class_name="BpDropdown",
        summary="The dropdown are a is a generic popup component that can be positioned relative to an anchor element.",
        props=(
            PropertySchema(
                name="position",
                kind="json",
                choices=(),
                type_text="Position",
                default="bottom",
                description="Controls the position of the dropdown relative to its anchor element",
            ),
            PropertySchema(
                name="closable",
                kind="boolean",
                choices=(),
                default="false",
                description="Controls whether the dropdown displays a close button, allowing users to dismiss it",
            ),
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
        ),
        events=("open", "close"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        position: Any = None,
        closable: bool | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "position": position,
                "closable": closable,
                "i18n": i18n,
            },
            **props,
        )


class BpFile(Component):
    """The file input component allows users to select and upload one or multiple files from their local device."""

    tag = "bp-file"
    schema = ComponentSchema(
        tag="bp-file",
        class_name="BpFile",
        summary="The file input component allows users to select and upload one or multiple files from their local device.",
        props=(
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
            PropertySchema(
                name="accept",
                kind="string",
                choices=(),
                default=None,
                description="Specifies the file types that the file input should accept, using MIME types or file extensions",
            ),
        ),
        events=("input", "change"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        i18n: Any = None,
        accept: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "i18n": i18n,
                "accept": accept,
            },
            **props,
        )


class BpFormatBytes(Component):
    """The format-bytes component is used to display byte values in a human-readable format with automatic unit conversion (b, kb, mb, gb, tb, pb)."""

    tag = "bp-format-bytes"
    schema = ComponentSchema(
        tag="bp-format-bytes",
        class_name="BpFormatBytes",
        summary="The format-bytes component is used to display byte values in a human-readable format with automatic unit conversion (b, kb, mb, gb, tb, pb).",
        props=(
            PropertySchema(
                name="display",
                kind="enum",
                choices=("decimal", "binary"),
                default="decimal",
                description="determines the base unit system: 'decimal' (1000-based) or 'binary' (1024-based)",
            ),
            PropertySchema(
                name="unit",
                kind="enum",
                choices=("b", "kb", "mb", "gb", "tb", "pb"),
                default=None,
                description="force a specific unit instead of auto-detection (b, kb, mb, gb, tb, pb)",
            ),
            PropertySchema(
                name="unit-display",
                kind="enum",
                choices=("long", "short"),
                default="short",
                description="determines how units are displayed: 'short' (kb), 'long' (kilobytes)",
            ),
            PropertySchema(
                name="locales", kind="json", choices=(), type_text="string[]", default=None, description="locales to use for number formatting"
            ),
            PropertySchema(
                name="minimum-fraction-digits", kind="number", choices=(), default="0", description="minimum number of fraction digits to display"
            ),
            PropertySchema(
                name="maximum-fraction-digits", kind="number", choices=(), default="2", description="maximum number of fraction digits to display"
            ),
            PropertySchema(
                name="value", kind="number", choices=(), default="0", description="Defines the numeric byte value to be formatted and displayed"
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        display: Literal["decimal", "binary"] | None = None,
        unit: Literal["b", "kb", "mb", "gb", "tb", "pb"] | None = None,
        unit_display: Literal["long", "short"] | None = None,
        locales: Any = None,
        minimum_fraction_digits: float | None = None,
        maximum_fraction_digits: float | None = None,
        value: float | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "display": display,
                "unit": unit,
                "unit-display": unit_display,
                "locales": locales,
                "minimum-fraction-digits": minimum_fraction_digits,
                "maximum-fraction-digits": maximum_fraction_digits,
                "value": value,
            },
            **props,
        )


class BpFormatDatetime(Component):
    """The format-datetime component is used to display a date and time in a human-readable format. The element API reflects the [Intl.DateTimeFormat](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/DateTimeFormat) API."""

    tag = "bp-format-datetime"
    schema = ComponentSchema(
        tag="bp-format-datetime",
        class_name="BpFormatDatetime",
        summary="The format-datetime component is used to display a date and time in a human-readable format. The element API reflects the [Intl.DateTimeFormat](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/DateTimeFormat) API.",
        props=(
            PropertySchema(name="locale", kind="string", choices=(), default=None, description="Specifies the locale for date and time formatting"),
            PropertySchema(
                name="weekday",
                kind="enum",
                choices=("long", "short", "narrow"),
                default=None,
                description="Controls the weekday representation in the formatted date",
            ),
            PropertySchema(
                name="year",
                kind="enum",
                choices=("numeric", "2-digit"),
                default=None,
                description="Controls the year representation in the formatted date",
            ),
            PropertySchema(
                name="month",
                kind="enum",
                choices=("numeric", "2-digit", "long", "short", "narrow"),
                default=None,
                description="Controls the month representation in the formatted date",
            ),
            PropertySchema(
                name="day",
                kind="enum",
                choices=("numeric", "2-digit"),
                default=None,
                description="Controls the day representation in the formatted date",
            ),
            PropertySchema(
                name="hour",
                kind="enum",
                choices=("numeric", "2-digit"),
                default=None,
                description="Controls the hour representation in the formatted time",
            ),
            PropertySchema(
                name="minute",
                kind="enum",
                choices=("numeric", "2-digit"),
                default=None,
                description="Controls the minute representation in the formatted time",
            ),
            PropertySchema(
                name="second",
                kind="enum",
                choices=("numeric", "2-digit"),
                default=None,
                description="Controls the second representation in the formatted time",
            ),
            PropertySchema(
                name="date-style",
                kind="enum",
                choices=("full", "long", "medium", "short"),
                default=None,
                description="Defines a preset formatting style for the date portion",
            ),
            PropertySchema(
                name="time-style",
                kind="enum",
                choices=("full", "long", "medium", "short"),
                default=None,
                description="Defines a preset formatting style for the time portion",
            ),
            PropertySchema(
                name="time-zone-name",
                kind="enum",
                choices=("long", "short"),
                default=None,
                description="Controls how the time zone name is displayed in the formatted datetime",
            ),
            PropertySchema(name="time-zone", kind="string", choices=(), default=None, description="Specifies the time zone to use for formatting"),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        locale: str | None = None,
        weekday: Literal["long", "short", "narrow"] | None = None,
        year: Literal["numeric", "2-digit"] | None = None,
        month: Literal["numeric", "2-digit", "long", "short", "narrow"] | None = None,
        day: Literal["numeric", "2-digit"] | None = None,
        hour: Literal["numeric", "2-digit"] | None = None,
        minute: Literal["numeric", "2-digit"] | None = None,
        second: Literal["numeric", "2-digit"] | None = None,
        date_style: Literal["full", "long", "medium", "short"] | None = None,
        time_style: Literal["full", "long", "medium", "short"] | None = None,
        time_zone_name: Literal["long", "short"] | None = None,
        time_zone: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "locale": locale,
                "weekday": weekday,
                "year": year,
                "month": month,
                "day": day,
                "hour": hour,
                "minute": minute,
                "second": second,
                "date-style": date_style,
                "time-style": time_style,
                "time-zone-name": time_zone_name,
                "time-zone": time_zone,
            },
            **props,
        )


class BpFormatNumber(Component):
    """The format-datetime component is used to display numbers in a human-readable format. The element reflects the [Intl.NumberFormat](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/NumberFormat) API."""

    tag = "bp-format-number"
    schema = ComponentSchema(
        tag="bp-format-number",
        class_name="BpFormatNumber",
        summary="The format-datetime component is used to display numbers in a human-readable format. The element reflects the [Intl.NumberFormat](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/NumberFormat) API.",
        props=(
            PropertySchema(
                name="format",
                kind="enum",
                choices=("currency", "decimal", "percent"),
                default="decimal",
                description="Defines the number formatting style, such as currency, decimal, or percent",
            ),
            PropertySchema(
                name="currency", kind="string", choices=(), default=None, description="Specifies the currency code to use for currency formatting"
            ),
            PropertySchema(
                name="locales",
                kind="json",
                choices=(),
                type_text="string[]",
                default=None,
                description="Specifies the locales to use for number formatting",
            ),
            PropertySchema(
                name="currency-sign",
                kind="enum",
                choices=("standard", "accounting"),
                default=None,
                description="Controls how to display the currency sign in accounting or standard format",
            ),
            PropertySchema(
                name="currency-display",
                kind="enum",
                choices=("symbol", "code", "name"),
                default=None,
                description="Controls how the currency is displayed, as symbol, code, or name",
            ),
            PropertySchema(
                name="compact-display",
                kind="enum",
                choices=("short", "long"),
                default=None,
                description="Controls how compact notation displays, using short or long forms",
            ),
            PropertySchema(
                name="unit-display",
                kind="enum",
                choices=("long", "short", "narrow"),
                default=None,
                description="Controls how units are displayed in formatted numbers",
            ),
            PropertySchema(
                name="notation",
                kind="enum",
                choices=("standard", "scientific", "engineering", "compact"),
                default=None,
                description="Defines the number notation style, such as standard, scientific, or compact",
            ),
            PropertySchema(
                name="sign-display",
                kind="enum",
                choices=("auto", "never", "always", "exceptZero"),
                default=None,
                description="Controls when to display the sign for positive and negative numbers",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        format: Literal["currency", "decimal", "percent"] | None = None,
        currency: str | None = None,
        locales: Any = None,
        currency_sign: Literal["standard", "accounting"] | None = None,
        currency_display: Literal["symbol", "code", "name"] | None = None,
        compact_display: Literal["short", "long"] | None = None,
        unit_display: Literal["long", "short", "narrow"] | None = None,
        notation: Literal["standard", "scientific", "engineering", "compact"] | None = None,
        sign_display: Literal["auto", "never", "always", "exceptZero"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "format": format,
                "currency": currency,
                "locales": locales,
                "currency-sign": currency_sign,
                "currency-display": currency_display,
                "compact-display": compact_display,
                "unit-display": unit_display,
                "notation": notation,
                "sign-display": sign_display,
            },
            **props,
        )


class BpFormatRelativeTime(Component):
    """The format-relative-time component displays relative time (e.g., "2 hours ago", "in 3 days") using the Intl.RelativeTimeFormat API."""

    tag = "bp-format-relative-time"
    schema = ComponentSchema(
        tag="bp-format-relative-time",
        class_name="BpFormatRelativeTime",
        summary='The format-relative-time component displays relative time (e.g., "2 hours ago", "in 3 days") using the Intl.RelativeTimeFormat API.',
        props=(
            PropertySchema(
                name="numeric",
                kind="enum",
                choices=("always", "auto"),
                default="auto",
                description='determines how to format the time: \'auto\' shows "yesterday" vs "1 day ago"',
            ),
            PropertySchema(
                name="format-style",
                kind="enum",
                choices=("long", "short", "narrow"),
                default="long",
                description="determines the formatting style: 'long', 'short', or 'narrow'",
            ),
            PropertySchema(
                name="unit",
                kind="enum",
                choices=("second", "minute", "hour", "day", "week", "month", "year", "auto"),
                default="auto",
                description="the time unit to use, or 'auto' to automatically select the best unit",
            ),
            PropertySchema(
                name="sync", kind="boolean", choices=(), default="false", description="auto-update the displayed time at appropriate intervals"
            ),
            PropertySchema(name="locale", kind="string", choices=(), default=None, description="locale to use for formatting"),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        numeric: Literal["always", "auto"] | None = None,
        format_style: Literal["long", "short", "narrow"] | None = None,
        unit: Literal["second", "minute", "hour", "day", "week", "month", "year", "auto"] | None = None,
        sync: bool | None = None,
        locale: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "numeric": numeric,
                "format-style": format_style,
                "unit": unit,
                "sync": sync,
                "locale": locale,
            },
            **props,
        )


class BpFormatToken(Component):
    """The format token component visualizes text tokenization for language models, displaying how text is split into tokens using various tokenization strategies like WordPiece, BPE, SentencePiece, and LLaMA."""

    tag = "bp-format-token"
    schema = ComponentSchema(
        tag="bp-format-token",
        class_name="BpFormatToken",
        summary="The format token component visualizes text tokenization for language models, displaying how text is split into tokens using various tokenization strategies like WordPiece, BPE, SentencePiece, and LLaMA.",
        props=(
            PropertySchema(
                name="format",
                kind="enum",
                choices=("bpe", "word-piece", "sentence-piece", "llama", "character", "whitespace"),
                default="bpe",
                description="Specifies the tokenization strategy used to split text into tokens for language model visualization",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        format: Literal["bpe", "word-piece", "sentence-piece", "llama", "character", "whitespace"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "format": format,
            },
            **props,
        )


class BpFieldMessage(Component):
    """Control Message ```typescript import '@blueprintui/components/include/forms.js'; ``` ```html <bp-field> <label>Test</label> <input type="text" /> <bp-field-message>helper text</bp-field-message> </bp-field> ```"""

    tag = "bp-field-message"
    schema = ComponentSchema(
        tag="bp-field-message",
        class_name="BpFieldMessage",
        summary="Control Message\n\n```typescript\nimport '@blueprintui/components/include/forms.js';\n```\n\n```html\n<bp-field>\n  <label>Test</label>\n  <input type=\"text\" />\n  <bp-field-message>helper text</bp-field-message>\n</bp-field>\n```",
        props=(
            PropertySchema(
                name="status", kind="enum", choices=("error", "success"), default=None, description="Set the status of field message validation"
            ),
            PropertySchema(
                name="error",
                kind="json",
                choices=(),
                type_text="keyof ValidityState",
                default=None,
                description="HTML5 ValidityState https://developer.mozilla.org/en-US/docs/Web/API/ValidityState",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        status: Literal["error", "success"] | None = None,
        error: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "status": status,
                "error": error,
            },
            **props,
        )


class BpField(Component):
    """```typescript import '@blueprintui/components/include/forms.js'; ``` ```html <bp-field> <label>field</label> <input type="text" /> </bp-field> ```"""

    tag = "bp-field"
    schema = ComponentSchema(
        tag="bp-field",
        class_name="BpField",
        summary="\n```typescript\nimport '@blueprintui/components/include/forms.js';\n```\n\n```html\n<bp-field>\n  <label>field</label>\n  <input type=\"text\" />\n</bp-field>\n```",
        props=(
            PropertySchema(name="layout", kind="json", choices=(), type_text="ControlLayout", default="vertical", description=None),
            PropertySchema(name="control-width", kind="enum", choices=("shrink",), default=None, description=None),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        layout: Any = None,
        control_width: Literal["shrink"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "layout": layout,
                "control-width": control_width,
            },
            **props,
        )


class BpFieldset(Component):
    """Control Group ```typescript import '@blueprintui/components/include/forms.js'; ``` ```html <bp-fieldset> <label>...</label> <label></label> <input ... /> <label></label> <input ... /> </bp-fieldset> ```"""

    tag = "bp-fieldset"
    schema = ComponentSchema(
        tag="bp-fieldset",
        class_name="BpFieldset",
        summary="Control Group\n\n```typescript\nimport '@blueprintui/components/include/forms.js';\n```\n\n```html\n<bp-fieldset>\n  <label>...</label>\n\n  <label></label>\n  <input ... />\n\n  <label></label>\n  <input ... />\n</bp-fieldset>\n```",
        props=(
            PropertySchema(name="layout", kind="json", choices=(), type_text="FormLayout", default="vertical", description="determine field layout"),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        layout: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "layout": layout,
            },
            **props,
        )


class BpFormGroup(Component):
    """Form ```typescript import '@blueprintui/components/include/forms.js'; ``` ```html <bp-form-group layout="horizontal"> <bp-input> <label>Test</label> <input type="text" /> </bp-input> <bp-input> <label>Test</label> <input type="text" /> </bp-input> </bp-form-group> ```"""

    tag = "bp-form-group"
    schema = ComponentSchema(
        tag="bp-form-group",
        class_name="BpFormGroup",
        summary='Form\n\n```typescript\nimport \'@blueprintui/components/include/forms.js\';\n```\n\n```html\n<bp-form-group layout="horizontal">\n  <bp-input>\n    <label>Test</label>\n    <input type="text" />\n  </bp-input>\n\n  <bp-input>\n    <label>Test</label>\n    <input type="text" />\n  </bp-input>\n</bp-form-group>\n```',
        props=(
            PropertySchema(
                name="layout",
                kind="json",
                choices=(),
                type_text="horizontal | horizontal",
                default="vertical",
                description="-inline | vertical | vertical-inline | compact}",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        layout: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "layout": layout,
            },
            **props,
        )


class BpHeader(Component):
    """The header component is used to provide a consistent and recognizable top bar, typically containing a logo and main navigation."""

    tag = "bp-header"
    schema = ComponentSchema(
        tag="bp-header",
        class_name="BpHeader",
        summary="The header component is used to provide a consistent and recognizable top bar, typically containing a logo and main navigation.",
        props=(),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpHeaderItem(Component):
    """Header Item"""

    tag = "bp-header-item"
    schema = ComponentSchema(
        tag="bp-header-item",
        class_name="BpHeaderItem",
        summary="Header Item",
        props=(
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpInput(Component):
    """The text input component is used to allow users to input and edit text. The text input should have a clear, descriptive label that explains what the user should enter as well as a placeholder text that gives an example of the expected input format."""

    tag = "bp-input"
    schema = ComponentSchema(
        tag="bp-input",
        class_name="BpInput",
        summary="The text input component is used to allow users to input and edit text. The text input should have a clear, descriptive label that explains what the user should enter as well as a placeholder text that gives an example of the expected input format.",
        props=(
            PropertySchema(
                name="type", kind="string", choices=(), default="text", description="Specifies the input type, affecting behavior and validation"
            ),
        ),
        events=("input", "change"),
        slots=("prefix", "suffix"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
            },
            **props,
        )


class BpMenu(Component):
    """The menu component provides a hierarchical view of available options, allowing users to choose from a variety of actions. Each option should have clear, descriptive text that helps the user understand what the option does."""

    tag = "bp-menu"
    schema = ComponentSchema(
        tag="bp-menu",
        class_name="BpMenu",
        summary="The menu component provides a hierarchical view of available options, allowing users to choose from a variety of actions. Each option should have clear, descriptive text that helps the user understand what the option does.",
        props=(),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpMenuItem(Component):
    """Menu Item"""

    tag = "bp-menu-item"
    schema = ComponentSchema(
        tag="bp-menu-item",
        class_name="BpMenuItem",
        summary="Menu Item",
        props=(
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpMonth(Component):
    """The month input component is used to allow the user to select a month from a dropdown menu. It is used as a replacement for traditional text input when selecting a month."""

    tag = "bp-month"
    schema = ComponentSchema(
        tag="bp-month",
        class_name="BpMonth",
        summary="The month input component is used to allow the user to select a month from a dropdown menu. It is used as a replacement for traditional text input when selecting a month.",
        props=(
            PropertySchema(
                name="type", kind="string", choices=(), default="month", description="Specifies the input type as month for date selection"
            ),
        ),
        events=("input", "change"),
        slots=("prefix", "suffix"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
            },
            **props,
        )


class BpNav(Component):
    """The Navigation component that allows users to navigate between different sections or pages. The component should contain links to different sections or pages within the application."""

    tag = "bp-nav"
    schema = ComponentSchema(
        tag="bp-nav",
        class_name="BpNav",
        summary="The Navigation component that allows users to navigate between different sections or pages. The component should contain links to different sections or pages within the application.",
        props=(
            PropertySchema(
                name="expanded",
                kind="boolean",
                choices=(),
                default="false",
                description="Controls whether the navigation is expanded, showing all items and content",
            ),
            PropertySchema(
                name="expandable",
                kind="boolean",
                choices=(),
                default="false",
                description="Determines if the navigation can be expanded or collapsed by the user",
            ),
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
            PropertySchema(
                name="interaction",
                kind="enum",
                choices=("auto",),
                default=None,
                description="Controls whether the component automatically manages expanded state based on user interactions",
            ),
        ),
        events=("open", "close"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        expanded: bool | None = None,
        expandable: bool | None = None,
        i18n: Any = None,
        interaction: Literal["auto"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "expanded": expanded,
                "expandable": expandable,
                "i18n": i18n,
                "interaction": interaction,
            },
            **props,
        )


class BpNavGroup(Component):
    """Button ```typescript import '@blueprintui/components/include/nav.js'; ``` ```html <bp-nav-group>submit</bp-nav-group> ```"""

    tag = "bp-nav-group"
    schema = ComponentSchema(
        tag="bp-nav-group",
        class_name="BpNavGroup",
        summary="Button\n\n```typescript\nimport '@blueprintui/components/include/nav.js';\n```\n\n```html\n<bp-nav-group>submit</bp-nav-group>\n```",
        props=(
            PropertySchema(name="expanded", kind="boolean", choices=(), default="false", description="determine if element is expanded"),
            PropertySchema(
                name="interaction", kind="enum", choices=("auto",), default=None, description="determine if element should auto manage expanded state"
            ),
        ),
        events=("open", "close"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        expanded: bool | None = None,
        interaction: Literal["auto"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "expanded": expanded,
                "interaction": interaction,
            },
            **props,
        )


class BpNavItem(Component):
    """Nav Item"""

    tag = "bp-nav-item"
    schema = ComponentSchema(
        tag="bp-nav-item",
        class_name="BpNavItem",
        summary="Nav Item",
        props=(
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpNumberStepper(Component):
    """A number input with explicit increment/decrement buttons in a horizontal minus-input-plus layout"""

    tag = "bp-number-stepper"
    schema = ComponentSchema(
        tag="bp-number-stepper",
        class_name="BpNumberStepper",
        summary="A number input with explicit increment/decrement buttons in a horizontal minus-input-plus layout",
        props=(
            PropertySchema(name="continuous", kind="boolean", choices=(), default="false", description="enable hold-to-repeat on stepper buttons"),
            PropertySchema(
                name="continuous-delay", kind="number", choices=(), default="500", description="delay in ms before continuous stepping starts"
            ),
            PropertySchema(
                name="continuous-interval", kind="number", choices=(), default="100", description="interval in ms for continuous stepping"
            ),
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                type_text="I18nStrings['actions']",
                default=None,
                description="Provides internationalization strings for accessibility labels and screen reader announcements",
            ),
        ),
        fields=(
            PropertySchema(name="value", kind="number", choices=(), default="0", description=None),
            PropertySchema(name="step", kind="number", choices=(), default="1", description=None),
        ),
        events=("input", "change"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        continuous: bool | None = None,
        continuous_delay: float | None = None,
        continuous_interval: float | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "continuous": continuous,
                "continuous-delay": continuous_delay,
                "continuous-interval": continuous_interval,
                "i18n": i18n,
            },
            **props,
        )


class BpNumber(Component):
    """The number input component is used to allow users to input numeric values with optional min, max, and step constraints."""

    tag = "bp-number"
    schema = ComponentSchema(
        tag="bp-number",
        class_name="BpNumber",
        summary="The number input component is used to allow users to input numeric values with optional min, max, and step constraints.",
        props=(
            PropertySchema(
                name="type", kind="string", choices=(), default="number", description="Specifies the input type as number for numeric input behavior"
            ),
        ),
        events=("input", "change"),
        slots=("prefix", "suffix"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
            },
            **props,
        )


class BpPage(Component):
    """The page component is for page level layout."""

    tag = "bp-page"
    schema = ComponentSchema(
        tag="bp-page",
        class_name="BpPage",
        summary="The page component is for page level layout.",
        props=(),
        events=(),
        slots=("", "header", "subheader", "aside-start", "aside-end", "subfooter", "footer"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpPagination(Component):
    """The Pagination component is used to navigate through a large number of pages. It allows users to easily switch between pages by clicking on page numbers or navigating to the first, previous, next, or last page."""

    tag = "bp-pagination"
    schema = ComponentSchema(
        tag="bp-pagination",
        class_name="BpPagination",
        summary="The Pagination component is used to navigate through a large number of pages. It allows users to easily switch between pages by clicking on page numbers or navigating to the first, previous, next, or last page.",
        props=(PropertySchema(name="i18n", kind="json", choices=(), default=None, description="set default aria/i18n strings"),),
        events=(),
        slots=("", "first", "next", "prev", "last"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "i18n": i18n,
            },
            **props,
        )


class BpPaginationInput(Component):
    """Grid Pagination ```typescript import '@blueprintui/components/include/pagination.js'; ``` ```html <form> <bp-pagination-input name="pagination" value="1" max="500" size="10" size-options="[10, 50, 100]"></bp-pagination-input> </form> ```"""

    tag = "bp-pagination-input"
    schema = ComponentSchema(
        tag="bp-pagination-input",
        class_name="BpPaginationInput",
        summary='Grid Pagination\n\n```typescript\nimport \'@blueprintui/components/include/pagination.js\';\n```\n\n```html\n<form>\n  <bp-pagination-input name="pagination" value="1" max="500" size="10" size-options="[10, 50, 100]"></bp-pagination-input>\n</form>\n```',
        props=(
            PropertySchema(name="size-options", kind="json", choices=(), type_text="number[]", default=None, description=None),
            PropertySchema(name="i18n", kind="json", choices=(), default=None, description="set default aria/i18n strings"),
        ),
        fields=(
            PropertySchema(name="value", kind="number", choices=(), default=None, description="determines initial value of the control"),
            PropertySchema(
                name="max", kind="number", choices=(), default=None, description="defines the greatest value in the range of permitted values"
            ),
            PropertySchema(name="size", kind="number", choices=(), default=None, description="determines the current page size"),
        ),
        events=("input", "change"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        size_options: Any = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "size-options": size_options,
                "i18n": i18n,
            },
            **props,
        )


class BpPanel(Component):
    """The panel component is used to display secondary information, commonly navigation or actions."""

    tag = "bp-panel"
    schema = ComponentSchema(
        tag="bp-panel",
        class_name="BpPanel",
        summary="The panel component is used to display secondary information, commonly navigation or actions.",
        props=(
            PropertySchema(
                name="size",
                kind="enum",
                choices=("sm", "md", "lg"),
                default=None,
                description="Determines the size variant of the panel for different visual hierarchies",
            ),
            PropertySchema(
                name="closable",
                kind="boolean",
                choices=(),
                default="false",
                description="Determines whether a close button is displayed for dismissing the panel",
            ),
            PropertySchema(name="hidden", kind="boolean", choices=(), default="false", description="Controls the visibility state of the panel"),
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
        ),
        events=(),
        slots=("", "header", "footer"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        size: Literal["sm", "md", "lg"] | None = None,
        closable: bool | None = None,
        hidden: bool | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "size": size,
                "closable": closable,
                "hidden": hidden,
                "i18n": i18n,
            },
            **props,
        )


class BpPassword(Component):
    """The password input component is used to accept password input from users. It is a text input field with a toggle button to show or hide the password."""

    tag = "bp-password"
    schema = ComponentSchema(
        tag="bp-password",
        class_name="BpPassword",
        summary="The password input component is used to accept password input from users. It is a text input field with a toggle button to show or hide the password.",
        props=(
            PropertySchema(
                name="type", kind="string", choices=(), default="password", description="Specifies the input type as password for secure text entry"
            ),
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
        ),
        events=("input", "change"),
        slots=("prefix", "suffix"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
                "i18n": i18n,
            },
            **props,
        )


class BpPin(Component):
    """PIN input component for entering verification codes and PINs with auto-advance and paste support"""

    tag = "bp-pin"
    schema = ComponentSchema(
        tag="bp-pin",
        class_name="BpPin",
        summary="PIN input component for entering verification codes and PINs with auto-advance and paste support",
        props=(
            PropertySchema(name="length", kind="number", choices=(), default="4", description="Number of input fields (typically 4-8)"),
            PropertySchema(name="type", kind="enum", choices=("text", "number"), default="text", description="Input type for each field"),
            PropertySchema(name="mask", kind="boolean", choices=(), default="false", description="Obscure input like password fields"),
        ),
        fields=(PropertySchema(name="value", kind="string", choices=(), default=None, description="Complete pin value"),),
        events=("complete", "input", "change"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        length: float | None = None,
        type: Literal["text", "number"] | None = None,
        mask: bool | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "length": length,
                "type": type,
                "mask": mask,
            },
            **props,
        )


class BpProgressBar(Component):
    """The progress bar component displays the progress of a task, usually on a scale from 0 to 100%. It can be used to show the progress of a download, upload, form completion, or any other process that can be represented as a percentage."""

    tag = "bp-progress-bar"
    schema = ComponentSchema(
        tag="bp-progress-bar",
        class_name="BpProgressBar",
        summary="The progress bar component displays the progress of a task, usually on a scale from 0 to 100%. It can be used to show the progress of a download, upload, form completion, or any other process that can be represented as a percentage.",
        props=(
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
            PropertySchema(
                name="min",
                kind="number",
                choices=(),
                default="0",
                description="Defines the minimum value in the range of permitted values for the progress bar",
            ),
            PropertySchema(
                name="max",
                kind="number",
                choices=(),
                default="100",
                description="Defines the maximum value in the range of permitted values for the progress bar",
            ),
            PropertySchema(
                name="value",
                kind="number",
                choices=(),
                default=None,
                description="Defines the current progress value, or null/undefined for indeterminate state",
            ),
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type affecting color and semantic meaning",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        i18n: Any = None,
        min: float | None = None,
        max: float | None = None,
        value: float | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "i18n": i18n,
                "min": min,
                "max": max,
                "value": value,
                "status": status,
            },
            **props,
        )


class BpProgressCircle(Component):
    """The progress circle component displays a visual representation of progress in the form of a circle. It is useful for showing the progress of a task in a compact, easy-to-understand manner."""

    tag = "bp-progress-circle"
    schema = ComponentSchema(
        tag="bp-progress-circle",
        class_name="BpProgressCircle",
        summary="The progress circle component displays a visual representation of progress in the form of a circle. It is useful for showing the progress of a task in a compact, easy-to-understand manner.",
        props=(
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type affecting color and semantic meaning",
            ),
            PropertySchema(name="value", kind="number", choices=(), default=None, description="Defines the current progress value from 0 to 100"),
            PropertySchema(name="line", kind="number", choices=(), default="3", description="Controls the stroke width of the progress circle ring"),
            PropertySchema(
                name="size",
                kind="enum",
                choices=("sm", "md", "lg"),
                default=None,
                description="Determines the size variant of the component for different visual hierarchies",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        i18n: Any = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        value: float | None = None,
        line: float | None = None,
        size: Literal["sm", "md", "lg"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "i18n": i18n,
                "status": status,
                "value": value,
                "line": line,
                "size": size,
            },
            **props,
        )


class BpProgressDot(Component):
    """The Progress dot component is designed to indicate that a process is ongoing. It gives users feedback that the system is processing the task and that they need to wait."""

    tag = "bp-progress-dot"
    schema = ComponentSchema(
        tag="bp-progress-dot",
        class_name="BpProgressDot",
        summary="The Progress dot component is designed to indicate that a process is ongoing. It gives users feedback that the system is processing the task and that they need to wait.",
        props=(
            PropertySchema(
                name="size",
                kind="enum",
                choices=("sm", "lg"),
                default=None,
                description="Determines the size variant of the component for different visual hierarchies",
            ),
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        size: Literal["sm", "lg"] | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "size": size,
                "i18n": i18n,
            },
            **props,
        )


class BpRadio(Component):
    """Use the radio input component when you want the user to select a single option from a list of mutually exclusive options."""

    tag = "bp-radio"
    schema = ComponentSchema(
        tag="bp-radio",
        class_name="BpRadio",
        summary="Use the radio input component when you want the user to select a single option from a list of mutually exclusive options.",
        props=(),
        events=("change",),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpRange(Component):
    """The range input component is used to allow the user to select a value within a specified range of values."""

    tag = "bp-range"
    schema = ComponentSchema(
        tag="bp-range",
        class_name="BpRange",
        summary="The range input component is used to allow the user to select a value within a specified range of values.",
        props=(),
        events=("input", "change"),
        slots=("prefix", "suffix", ""),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpRating(Component):
    """The rating input component is used to allow the user to select a rating value within a specified range of values."""

    tag = "bp-rating"
    schema = ComponentSchema(
        tag="bp-rating",
        class_name="BpRating",
        summary="The rating input component is used to allow the user to select a rating value within a specified range of values.",
        props=(),
        events=("input", "change"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpSearch(Component):
    """The Search component is used for searching for specific content within an application."""

    tag = "bp-search"
    schema = ComponentSchema(
        tag="bp-search",
        class_name="BpSearch",
        summary="The Search component is used for searching for specific content within an application.",
        props=(
            PropertySchema(
                name="type",
                kind="string",
                choices=(),
                default="search",
                description="Specifies the input type as search for optimized search behavior",
            ),
        ),
        events=("input", "change"),
        slots=("prefix", "suffix"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
            },
            **props,
        )


class BpSelect(Component):
    """The select input component allows users to select an option from a dropdown list of options. The options are displayed when the user clicks on the select input."""

    tag = "bp-select"
    schema = ComponentSchema(
        tag="bp-select",
        class_name="BpSelect",
        summary="The select input component allows users to select an option from a dropdown list of options. The options are displayed when the user clicks on the select input.",
        props=(),
        events=("input", "change"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpOption(Component):
    """Option element for use within bp-select."""

    tag = "bp-option"
    schema = ComponentSchema(
        tag="bp-option", class_name="BpOption", summary="Option element for use within bp-select.", props=(), events=(), slots=()
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpSkeleton(Component):
    """Skeleton provides a placeholder representation of content before it loads"""

    tag = "bp-skeleton"
    schema = ComponentSchema(
        tag="bp-skeleton",
        class_name="BpSkeleton",
        summary="Skeleton provides a placeholder representation of content before it loads",
        props=(
            PropertySchema(
                name="effect", kind="enum", choices=("pulse", "sheen"), default=None, description="Animation effect applied to the skeleton"
            ),
            PropertySchema(name="shape", kind="enum", choices=("circle",), default=None, description="Shape of the skeleton"),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        effect: Literal["pulse", "sheen"] | None = None,
        shape: Literal["circle"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "effect": effect,
                "shape": shape,
            },
            **props,
        )


class BpStepper(Component):
    """Stepper components guide users through a multi-step process, breaking it down into manageable parts. This increases understanding, reduces cognitive load, and enhances the overall user experience."""

    tag = "bp-stepper"
    schema = ComponentSchema(
        tag="bp-stepper",
        class_name="BpStepper",
        summary="Stepper components guide users through a multi-step process, breaking it down into manageable parts. This increases understanding, reduces cognitive load, and enhances the overall user experience.",
        props=(
            PropertySchema(
                name="layout",
                kind="enum",
                choices=("horizontal", "vertical"),
                default="horizontal",
                description="Controls the layout direction of the stepper, either horizontal or vertical",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        layout: Literal["horizontal", "vertical"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "layout": layout,
            },
            **props,
        )


class BpStepperItem(Component):
    tag = "bp-stepper-item"
    schema = ComponentSchema(
        tag="bp-stepper-item",
        class_name="BpStepperItem",
        summary="",
        props=(
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="determine the visual status state",
            ),
            PropertySchema(name="selected", kind="boolean", choices=(), default="false", description="selected visual state"),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        selected: bool | None = None,
        disabled: bool | None = None,
        readonly: bool | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "status": status,
                "selected": selected,
                "disabled": disabled,
                "readonly": readonly,
            },
            **props,
        )


class BpSwitch(Component):
    """Use the switch input component for boolean-like options, such as enabling/disabling notifications, or for settings that can be toggled between two states."""

    tag = "bp-switch"
    schema = ComponentSchema(
        tag="bp-switch",
        class_name="BpSwitch",
        summary="Use the switch input component for boolean-like options, such as enabling/disabling notifications, or for settings that can be toggled between two states.",
        props=(),
        events=("input", "change"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpTabs(Component):
    """The tabs component is used to display a group of related content in a tabbed interface. The tabs component consists of a collection of tabs, where each tab represents a different piece of content."""

    tag = "bp-tabs"
    schema = ComponentSchema(
        tag="bp-tabs",
        class_name="BpTabs",
        summary="The tabs component is used to display a group of related content in a tabbed interface. The tabs component consists of a collection of tabs, where each tab represents a different piece of content.",
        props=(
            PropertySchema(
                name="layout",
                kind="enum",
                choices=("horizontal", "vertical"),
                default="horizontal",
                description="Controls the layout direction of the tabs, either horizontal or vertical",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        layout: Literal["horizontal", "vertical"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "layout": layout,
            },
            **props,
        )


class BpTabList(Component):
    """```typescript import '@blueprintui/components/include/tabs.js'; ``` ```html <bp-tab-list></bp-tab-list> ```"""

    tag = "bp-tab-list"
    schema = ComponentSchema(
        tag="bp-tab-list",
        class_name="BpTabList",
        summary="```typescript\nimport '@blueprintui/components/include/tabs.js';\n```\n\n```html\n<bp-tab-list></bp-tab-list>\n```",
        props=(),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpTabPanel(Component):
    """```typescript import '@blueprintui/components/include/tabs.js'; ``` ```html <bp-tab-panel></bp-tab-panel> ```"""

    tag = "bp-tab-panel"
    schema = ComponentSchema(
        tag="bp-tab-panel",
        class_name="BpTabPanel",
        summary="```typescript\nimport '@blueprintui/components/include/tabs.js';\n```\n\n```html\n<bp-tab-panel></bp-tab-panel>\n```",
        props=(),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpTab(Component):
    """```typescript import '@blueprintui/components/include/tabs.js'; ``` ```html <bp-tab></bp-tab> ```"""

    tag = "bp-tab"
    schema = ComponentSchema(
        tag="bp-tab",
        class_name="BpTab",
        summary="```typescript\nimport '@blueprintui/components/include/tabs.js';\n```\n\n```html\n<bp-tab></bp-tab>\n```",
        props=(
            PropertySchema(name="selected", kind="boolean", choices=(), default="false", description="selected visual state"),
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        fields=(PropertySchema(name="tabPanel", kind="json", choices=(), type_text="BpTabPanel", default=None, description=None),),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        selected: bool | None = None,
        pressed: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "selected": selected,
                "pressed": pressed,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpTag(Component):
    """The tag component is used to display labels or categories of information. It can be used to group and categorize similar items, or to label content with a certain type or status."""

    tag = "bp-tag"
    schema = ComponentSchema(
        tag="bp-tag",
        class_name="BpTag",
        summary="The tag component is used to display labels or categories of information. It can be used to group and categorize similar items, or to label content with a certain type or status.",
        props=(
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type affecting color and semantic meaning",
            ),
            PropertySchema(name="pressed", kind="boolean", choices=(), default=None, description="turns the button into a toggle button"),
            PropertySchema(
                name="selected", kind="boolean", choices=(), default=None, description='indicate the current "selected" state of various widgets'
            ),
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default="false", description="determines if element is mutable or focusable"),
            PropertySchema(name="type", kind="enum", choices=("button", "submit"), default=None, description="default behavior of the button"),
            PropertySchema(
                name="name", kind="string", choices=(), default=None, description="represents the name of the current <form> element as a string."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="determines the current value"),
            PropertySchema(
                name="popovertargetaction",
                kind="enum",
                choices=("toggle", "show", "hide"),
                default=None,
                description="The action to perform on the target popover element\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertargetaction",
            ),
            PropertySchema(
                name="popoverTargetElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The target element for the popover\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#popovertarget",
            ),
            PropertySchema(
                name="command",
                kind="string",
                choices=(),
                default=None,
                description="The command property of the HTMLButtonElement interface gets and sets the action to be performed on an element being controlled by this button. For this to have an effect, commandfor must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="commandForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The commandForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#command",
            ),
            PropertySchema(
                name="interestForElement",
                kind="json",
                choices=(),
                type_text="HTMLElement",
                default=None,
                description="The interestForElement property of the HTMLButtonElement interface gets and sets the id of the element being controlled by this button. For this to have an effect, command must be set.\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#interest",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        pressed: bool | None = None,
        selected: bool | None = None,
        expanded: bool | None = None,
        readonly: bool | None = None,
        disabled: bool | None = None,
        type: Literal["button", "submit"] | None = None,
        name: str | None = None,
        value: str | None = None,
        popovertargetaction: Literal["toggle", "show", "hide"] | None = None,
        popoverTargetElement: Any = None,
        command: str | None = None,
        commandForElement: Any = None,
        interestForElement: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "status": status,
                "pressed": pressed,
                "selected": selected,
                "expanded": expanded,
                "readonly": readonly,
                "disabled": disabled,
                "type": type,
                "name": name,
                "value": value,
                "popovertargetaction": popovertargetaction,
                "popoverTargetElement": popoverTargetElement,
                "command": command,
                "commandForElement": commandForElement,
                "interestForElement": interestForElement,
            },
            **props,
        )


class BpTelephone(Component):
    """The telephone input component allows users to input and edit telephone numbers. Uses native `<input type="tel">` internally for proper mobile keyboard support and native validation."""

    tag = "bp-telephone"
    schema = ComponentSchema(
        tag="bp-telephone",
        class_name="BpTelephone",
        summary='The telephone input component allows users to input and edit telephone numbers. Uses native `<input type="tel">` internally for proper mobile keyboard support and native validation.',
        props=(
            PropertySchema(
                name="type", kind="string", choices=(), default="tel", description="Specifies the input type, affecting behavior and validation"
            ),
        ),
        events=("input", "change"),
        slots=("prefix", "suffix"),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
            },
            **props,
        )


class BpTextarea(Component):
    """The textarea component is used to capture multi-line text input from the user."""

    tag = "bp-textarea"
    schema = ComponentSchema(
        tag="bp-textarea",
        class_name="BpTextarea",
        summary="The textarea component is used to capture multi-line text input from the user.",
        props=(),
        events=("input", "change"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(*children, key=key, **props)


class BpTime(Component):
    """The time input component is used to collect a specific time value from the user. It can be used as a standalone component or as part of a form."""

    tag = "bp-time"
    schema = ComponentSchema(
        tag="bp-time",
        class_name="BpTime",
        summary="The time input component is used to collect a specific time value from the user. It can be used as a standalone component or as part of a form.",
        props=(
            PropertySchema(name="type", kind="string", choices=(), default="time", description="Specifies the input type as time for time selection"),
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
        ),
        fields=(
            PropertySchema(
                name="min", kind="string", choices=(), default=None, description="Override min to preserve string time values (HH:MM format)"
            ),
            PropertySchema(
                name="max", kind="string", choices=(), default=None, description="Override max to preserve string time values (HH:MM format)"
            ),
        ),
        events=("input", "change"),
        slots=("prefix", "suffix", ""),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        type: str | None = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "type": type,
                "i18n": i18n,
            },
            **props,
        )


class BpToast(Component):
    """A Toast component is a notification that appears on the screen for a short period of time and provides feedback about an action performed by the user. A Toast should be used for short, non-interruptive notifications that are meant to supplement the current screen."""

    tag = "bp-toast"
    schema = ComponentSchema(
        tag="bp-toast",
        class_name="BpToast",
        summary="A Toast component is a notification that appears on the screen for a short period of time and provides feedback about an action performed by the user. A Toast should be used for short, non-interruptive notifications that are meant to supplement the current screen.",
        props=(
            PropertySchema(
                name="closable",
                kind="boolean",
                choices=(),
                default="false",
                description="Determines whether a close button is displayed for dismissing the toast",
            ),
            PropertySchema(
                name="position",
                kind="json",
                choices=(),
                type_text="Position",
                default="top",
                description="Specifies the position of the toast relative to its anchor or viewport",
            ),
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
            PropertySchema(
                name="status",
                kind="enum",
                choices=("accent", "success", "warning", "danger"),
                default=None,
                description="Defines the visual status type affecting color, icon, and semantic meaning",
            ),
        ),
        events=("open", "close"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        closable: bool | None = None,
        position: Any = None,
        i18n: Any = None,
        status: Literal["accent", "success", "warning", "danger"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "closable": closable,
                "position": position,
                "i18n": i18n,
                "status": status,
            },
            **props,
        )


class BpToggletip(Component):
    """The toggletip component is used to provide explanations, definitions, or supplementary interactions for a given element. It should be used to provide additional information or actions that are not essential to the user's understanding of the content, but that may be helpful or informative."""

    tag = "bp-toggletip"
    schema = ComponentSchema(
        tag="bp-toggletip",
        class_name="BpToggletip",
        summary="The toggletip component is used to provide explanations, definitions, or supplementary interactions for a given element. It should be used to provide additional information or actions that are not essential to the user's understanding of the content, but that may be helpful or informative.",
        props=(
            PropertySchema(
                name="closable",
                kind="boolean",
                choices=(),
                default="false",
                description="Determines whether a close button is displayed for dismissing the toggletip",
            ),
            PropertySchema(
                name="position",
                kind="json",
                choices=(),
                type_text="Position",
                default="top",
                description="Specifies the position of the toggletip relative to its anchor element",
            ),
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
        ),
        events=("open", "close"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        closable: bool | None = None,
        position: Any = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "closable": closable,
                "position": position,
                "i18n": i18n,
            },
            **props,
        )


class BpTooltip(Component):
    """The tooltip component is used to provide additional information to the user when they hover over a specific element. It can be used to provide explanations, definitions, or supplementary information that is not critical to the main content."""

    tag = "bp-tooltip"
    schema = ComponentSchema(
        tag="bp-tooltip",
        class_name="BpTooltip",
        summary="The tooltip component is used to provide additional information to the user when they hover over a specific element. It can be used to provide explanations, definitions, or supplementary information that is not critical to the main content.",
        props=(
            PropertySchema(
                name="closable",
                kind="boolean",
                choices=(),
                default="false",
                description="Determines whether a close button is displayed for dismissing the tooltip",
            ),
            PropertySchema(
                name="position",
                kind="json",
                choices=(),
                type_text="Position",
                default="top",
                description="Specifies the position of the tooltip relative to its anchor element",
            ),
            PropertySchema(
                name="i18n", kind="json", choices=(), default=None, description="Provides internationalization strings for translated text content"
            ),
        ),
        events=("open", "close"),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        closable: bool | None = None,
        position: Any = None,
        i18n: Any = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "closable": closable,
                "position": position,
                "i18n": i18n,
            },
            **props,
        )


class BpTree(Component):
    """The tree component enables hierarchical data organization, offering a collapsible and expandable tree structure. Users can easily navigate through parent and child nodes, expanding or collapsing branches as needed. The component supports for the highlighting of selected nodes."""

    tag = "bp-tree"
    schema = ComponentSchema(
        tag="bp-tree",
        class_name="BpTree",
        summary="The tree component enables hierarchical data organization, offering a collapsible and expandable tree structure. Users can easily navigate through parent and child nodes, expanding or collapsing branches as needed. The component supports for the highlighting of selected nodes.",
        props=(
            PropertySchema(
                name="interaction",
                kind="enum",
                choices=("auto",),
                default=None,
                description="Controls whether the component automatically manages item expansion and selection based on user interactions",
            ),
            PropertySchema(
                name="selectable",
                kind="enum",
                choices=("multi", "single"),
                default=None,
                description="Determines the selection mode for tree items, allowing single or multiple selections",
            ),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        interaction: Literal["auto"] | None = None,
        selectable: Literal["multi", "single"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "interaction": interaction,
                "selectable": selectable,
            },
            **props,
        )


class BpTreeItem(Component):
    tag = "bp-tree-item"
    schema = ComponentSchema(
        tag="bp-tree-item",
        class_name="BpTreeItem",
        summary="",
        props=(
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="indicate if a control is expanded or collapsed"),
            PropertySchema(name="selected", kind="boolean", choices=(), default="false", description="selected visual state"),
            PropertySchema(
                name="indeterminate", kind="boolean", choices=(), default="false", description="determines if node has some selected child nodes"
            ),
            PropertySchema(name="disabled", kind="boolean", choices=(), default=None, description="determines if element is mutable or focusable"),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="makes the element not mutable, meaning the user can not interact with button",
            ),
            PropertySchema(name="selectable", kind="enum", choices=("multi", "single"), default=None, description=None),
        ),
        events=(),
        slots=("",),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        expanded: bool | None = None,
        selected: bool | None = None,
        indeterminate: bool | None = None,
        disabled: bool | None = None,
        readonly: bool | None = None,
        selectable: Literal["multi", "single"] | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "expanded": expanded,
                "selected": selected,
                "indeterminate": indeterminate,
                "disabled": disabled,
                "readonly": readonly,
                "selectable": selectable,
            },
            **props,
        )


class BpIcon(Component):
    """Icon ```typescript import '@blueprintui/icons/include.js'; import '@blueprintui/icons/shapes/user.js'; ``` ```html <bp-icon shape="user"></bp-icon> ```"""

    tag = "bp-icon"
    schema = ComponentSchema(
        tag="bp-icon",
        class_name="BpIcon",
        summary="Icon\n\n```typescript\nimport '@blueprintui/icons/include.js';\nimport '@blueprintui/icons/shapes/user.js';\n```\n\n```html\n<bp-icon shape=\"user\"></bp-icon>\n```",
        props=(
            PropertySchema(name="shape", kind="string", choices=(), default="unknown", description=None),
            PropertySchema(name="type", kind="enum", choices=("", "solid"), default=None, description=None),
            PropertySchema(name="size", kind="string", choices=(), default=None, description="determine the visual size state"),
            PropertySchema(
                name="status",
                kind="enum",
                choices=("neutral", "accent", "success", "warning", "danger"),
                default=None,
                description="determine the visual status state",
            ),
            PropertySchema(
                name="badge", kind="enum", choices=("neutral", "accent", "success", "warning", "danger", ""), default=None, description=None
            ),
            PropertySchema(name="direction", kind="json", choices=(), type_text="Directions", default=None, description=None),
            PropertySchema(name="inner-offset", kind="number", choices=(), default=None, description=None),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        shape: str | None = None,
        type: Literal["", "solid"] | None = None,
        size: str | None = None,
        status: Literal["neutral", "accent", "success", "warning", "danger"] | None = None,
        badge: Literal["neutral", "accent", "success", "warning", "danger", ""] | None = None,
        direction: Any = None,
        inner_offset: float | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "shape": shape,
                "type": type,
                "size": size,
                "status": status,
                "badge": badge,
                "direction": direction,
                "inner-offset": inner_offset,
            },
            **props,
        )
